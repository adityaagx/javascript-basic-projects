# 11 – Expense Tracker

A simple Expense Tracker built with Vanilla JavaScript.

This project allows users to add, delete, and track expenses while automatically calculating the total amount. All data is saved using `localStorage`, so it persists even after refreshing the page.

---

## 🎯 Project Goal

Build an interactive expense management app that:

- Accepts expense name and amount
- Displays a list of all expenses
- Calculates total expenses dynamically
- Allows deleting individual expenses
- Saves data in localStorage

---